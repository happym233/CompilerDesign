function main() => void                                           // 2.1, 2.2
{
  localvar counter: float;                                        // 1.1
  counter = 1.0;                                                    // 3.1
  write(counter * 2.0);
  write(counter <= 1.2);
}
